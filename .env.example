PORT=3000
NODE_ENV=production
BASE_URL=https://yourdomain.com
DB_HOST={CHANGE_IN_DOCKER}
DB_PORT=3306
DB_USER=bloguser
DB_PASSWORD=blogpass
DB_NAME=myshkin451blog
# 必须修改！应用启动时会校验此变量，未设置将拒绝启动
# 生成方式: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=
